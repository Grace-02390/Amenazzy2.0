# Configuración para producción
spring.application.name=demo

# Configuración de la base de datos (usar variables de entorno)
spring.datasource.url=${DATABASE_URL:jdbc:mysql://sql10.freesqldatabase.com:3306/sql10789373}
spring.datasource.username=${DATABASE_USERNAME:sql10789373}
spring.datasource.password=${DATABASE_PASSWORD:BwFfZLar1e}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Puerto del servidor
server.port=${PORT:8080}
spring.main.allow-circular-references=true

# Configuración de seguridad para producción
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin123}

# Configuración de JWT
jwt.secret=${JWT_SECRET:your-secret-key-here}
jwt.expiration=${JWT_EXPIRATION:86400000} 